

/*(question)
Implement an AVL tree (using linked list) the AVL object needs these methods:
    insert(data) //新增節點
    getNodeHeight(node) //計算平衡因子需要節點高度
    getBF(node) //計算平衡因子
    rightRotation(node) //右轉
    leftRotation(node) //左轉
    isBalance(node) //對不平衡的節點調整
    remove(data) //刪除節點
*/