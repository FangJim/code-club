/**
 * @param {number[]} position
 * @return {number}
 */
var minCostToMoveChips = function (position) {
  let positionChip = [];
  let chipNumber = 1;
  let j = 0;
  for (let i = 0; i < position.length; i++) {}
  console.log(positionChip);
};
let position = [1, 2, 3];
minCostToMoveChips(position);
